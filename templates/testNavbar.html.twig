<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand" href="/activite">ActivitéLand</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
            {% if is_granted("ROLE_ENFANT") %}
            <li class="nav-item">
                <a class="nav-link" href="{{ path('activite_listes_inscription')}}">Listes</a>
            </li>
            {% endif %}
            {% if is_granted("ROLE_ADMIN") %}
                <li class="nav-item">
                    <a class="nav-link" href="/admin">Administrateur</a>
                </li>
            {% endif %}
        </ul>
        <ul class="navbar-nav flex-fill w-100 justify-content-end">
            {% if app.user %}
                <li class="nav-item right-text-pad">
                    <a class="nav-link" href="{{ path('app_logout') }}">Déconnexion</a>
                </li>
            {% else %}
                <li class="nav-item right-text-pad">
                    <a class="nav-link" href="{{ path('app_login') }}">Connexion</a>
                </li>
                <li class="nav-item right-text-pad">
                    <a class="nav-link" href="/register">Inscription</a>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>
